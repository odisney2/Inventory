@*/*
            Orris Disney II
            C# Inventory Project
            July 2018

            Definition of a View
            View is a user interface. View displays data from the model to the user and also enables them to modify the data

            This is for the Item Type View Detail page.  This will show the data for the specific item type that the user clicked on

            The majority of the page layout is stored in the Shared/_Layout.cshtml file
    */*@

@model Inventory.Models.ItemTypeViewModel
@{
    ViewBag.Title = "ItemType Detail";
}

<h2>@Model.ItemTypeDisplayName</h2>

<div class="row">
    <div class="col-sm-12 form-group">
        @Html.LabelFor(m => m.ItemTypeDisplayName)
        <br />@Html.DisplayFor(m => m.ItemTypeDisplayName)
    </div>
    <div class="col-sm-12 form-group">
        @Html.LabelFor(m => m.ItemTypeNotes)
        <br />@Html.DisplayFor(m => m.ItemTypeNotes)
    </div>
    <div class="col-sm-12">
        @Html.ActionLink("Done", "Index", null, new { @class = "btn btn-primary" })
    </div>
</div>